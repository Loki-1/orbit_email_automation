# ORBIT Email Automation — Python dependencies
# Install with:  pip install -r requirements.txt

pandas>=2.0
openpyxl>=3.1      # pandas Excel engine
pywin32>=305       # Only required on Windows when SEND_METHOD = "OUTLOOK"
